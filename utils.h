#ifndef _UTILS_H
#define _UTILS_H

#include <stddef.h>

typedef int s32;

typedef size_t usize;
typedef ssize_t ssize;

typedef float f32;
typedef double f64;

#define KB(x) (1024*(x))
#define MB(x) (1024*KB(x))
#define GB(x) (1024*MB(x))

#endif
